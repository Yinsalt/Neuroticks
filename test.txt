# Erst die fehlenden Basics
sudo apt install -y python3.12-venv python3-pip git

# Dann die OpenGL/Mesa Pakete (neue Namen für Ubuntu 24.04)
sudo apt install -y \
    libgl1 \
    libegl1 \
    libxcb-xinerama0 libxcb-cursor0 \
    libxkbcommon-x11-0 libdbus-1-3 \
    libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
    libxcb-randr0 libxcb-render-util0 libxcb-shape0 \
    mesa-utils

# Dann weiter mit venv
cd ~
python3 -m venv nest_env
source nest_env/bin/activate
pip install --upgrade pip
